---
interface Props {
  slot: 'leaderboard' | 'rectangle' | 'sidebar';
  id: string;
}

const { slot, id } = Astro.props;
---

<!-- Mediavine ad slot: activate when traffic threshold reached -->
<div data-ad={slot} id={`ad-${id}`} aria-hidden="true"></div>
