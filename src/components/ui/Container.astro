---
interface Props {
  class?: string;
  wide?: boolean;
}

const { class: className, wide = false } = Astro.props;

const maxWidth = wide ? 'max-w-wide' : 'max-w-content';

const classes = [maxWidth, 'mx-auto px-4 sm:px-6 lg:px-8', className]
  .filter(Boolean)
  .join(' ');
---

<div class={classes}>
  <slot />
</div>
